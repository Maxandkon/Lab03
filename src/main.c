#include <stdio.h>
int main()
{
	int nt, nj, c; //Змінні
	nt = 0;
	nj = 13; //Номер студента
	c = 6; //Кількість варіантів у лабороторній роботі
	nt = ((nj - 1) % c) + 1; //Формула для виведення результату
	putchar(nt);
}
